---
import { generateRoseyId } from "rosey-cloudcannon-connector/utils";

const block = Astro.props;
---

<a
  href={block.src}
  download={block.file_name}
  data-rosey={generateRoseyId(block.link_message)}>
  {block.link_message}
</a>
